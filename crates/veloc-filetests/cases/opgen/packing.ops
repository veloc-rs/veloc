// ----- successor-editor-name-is-reserved
// run: opgen-error
// check: conflicting draft constructor `edit_successors`
struct EditSuccessors {

}
op EditSuccessors() -> () { meta: OpInfo { memory: Known([]) }, mnemonic: "editsuccessors", storage: EditSuccessors {} }

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-1
// run: opgen-error
// check: expected parameter, array, pool(parameter) or table(cases, default)
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: PtrIndexImm) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: pool() },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-2
// run: opgen-error
// check: parameter `imm` is incompatible with storage field `imm_id`
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: Bytes) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-3
// run: opgen-error
// check: parameter `unused` has no storage mapping
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: PtrIndexImm, @unused: PtrIndexImm) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-4
// run: opgen-error
// check: parameter `imm` is stored more than once
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
    other: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: PtrIndexImm) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm, other: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-5
// run: opgen-error
// check: unknown property type `PtrIndexImmId`
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: PtrIndexImmId) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-6
// run: opgen-error
// check: unknown property type `ConstantPoolId`
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: ConstantPoolId) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-7
// run: opgen-error
// check: unknown property type `VectorExtId`
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: VectorExtId) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pool-bindings-are-typed-and-consume-the-complete-struct-8
// run: opgen-error
// check: unknown property type `VectorMemExtId`
struct PtrIndexImm {
    offset: i32,
    scale: u32,
}
struct Indexed {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}
op Indexed(ptr: PTR, index: I32, @imm: VectorMemExtId) -> (result: PTR) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "indexed", storage: Indexed { ptr: ptr, index: index, imm_id: imm },
}

// ----- pooled-value-lists-and-inline-arrays-keep-distinct-bindings-1
// run: opgen-error
// check: storage field `args` requires 3 arguments
struct Triple { args: values(3) }
op Triple<T: Integer>(a: T, b: T, c: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
mnemonic: "triple", storage: Triple { args: [a, b] }, }

// ----- pooled-value-lists-and-inline-arrays-keep-distinct-bindings-2
// run: opgen-error
// check: parameter `a` is stored more than once
struct Triple { args: values(3) }
op Triple<T: Integer>(a: T, b: T, c: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
mnemonic: "triple", storage: Triple { args: [a, a, c] }, }

// ----- signature-sources-are-explicit-and-match-their-parameter-types-1
// run: opgen-error
// check: signature results require an explicit signature source
struct Invoke {
    callee: FuncId,
    args: ValueList,
}
op Invoke(@callee: FuncId, args: values) -> signature {
    meta: OpInfo { memory: Unknown },
    mnemonic: "invoke", storage: Invoke { callee: callee, args: args },
}

// ----- signature-sources-are-explicit-and-match-their-parameter-types-2
// run: opgen-error
// check: signature source `callee` must be a SigId property
struct Invoke {
    callee: FuncId,
    args: ValueList,
}
op Invoke(@callee: FuncId, args: values) -> signature {
    meta: OpInfo { memory: Unknown },
    mnemonic: "invoke", storage: Invoke { callee: callee, args: args },
signature: callee, }

// ----- signature-sources-are-explicit-and-match-their-parameter-types-3
// run: opgen-error
// check: signature source `missing` must be a FuncId property
struct Invoke {
    callee: FuncId,
    args: ValueList,
}
op Invoke(@callee: FuncId, args: values) -> signature {
    meta: OpInfo { memory: Unknown },
    mnemonic: "invoke", storage: Invoke { callee: callee, args: args },
signature: function(missing), }

// ----- signature-sources-are-explicit-and-match-their-parameter-types-4
// run: opgen-error
// check: signature source requires signature results
struct Invoke {
    callee: FuncId,
    args: ValueList,
}
op Invoke(@callee: FuncId, args: values) -> () {
    meta: OpInfo { memory: Unknown },
    mnemonic: "invoke", storage: Invoke { callee: callee, args: args },
signature: function(callee), }

// ----- tables-require-distinct-case-and-default-roles-1
// run: opgen-error
// check: expected parameter, array, pool(parameter) or table(cases, default)
struct Table {
    index: Value,
    table: JumpTable,
}
op Table(index: I32, cases: successors, default: successor) -> () {
    meta: OpInfo { traits: [TERMINATOR], memory: Known([]) },
    mnemonic: "table", storage: Table { index: index, table: table(cases) },
}

// ----- tables-require-distinct-case-and-default-roles-2
// run: opgen-error
// check: parameter `cases` is stored more than once
struct Table {
    index: Value,
    table: JumpTable,
}
op Table(index: I32, cases: successors, default: successor) -> () {
    meta: OpInfo { traits: [TERMINATOR], memory: Known([]) },
    mnemonic: "table", storage: Table { index: index, table: table(cases, cases) },
}

// ----- tables-require-distinct-case-and-default-roles-3
// run: opgen-error
// check: parameter `default` is incompatible with storage field `table`
struct Table {
    index: Value,
    table: JumpTable,
}
op Table(index: I32, cases: successors, default: successors) -> () {
    meta: OpInfo { traits: [TERMINATOR], memory: Known([]) },
    mnemonic: "table", storage: Table { index: index, table: table(cases, default) },
}

// ----- tables-require-distinct-case-and-default-roles-4
// run: opgen-error
// check: parameter `cases` is incompatible with storage field `table`
struct Table {
    index: Value,
    table: JumpTable,
}
op Table(index: I32, cases: successors, default: successor) -> () {
    meta: OpInfo { traits: [TERMINATOR], memory: Known([]) },
    mnemonic: "table", storage: Table { index: index, table: cases },
}

// ----- draft-constructor-name-collision
// run: opgen-error
// check: conflicting draft constructor `a_b`
struct AB {  }
op AB() -> () { meta: OpInfo { memory: Known([]) }, mnemonic: "ab", storage: AB {} }
struct A_b {  }
op A_b() -> () { meta: OpInfo { memory: Known([]) }, mnemonic: "a_b", storage: A_b {} }

// ----- draft-constructor-api-collision
// run: opgen-error
// check: conflicting draft constructor `operands`
struct Operands {  }
op Operands() -> () { meta: OpInfo { memory: Known([]) }, mnemonic: "operands", storage: Operands {} }

// ----- draft-constructor-keyword
// run: opgen-error
// check: invalid generated identifier `match`
struct Match {  }
op Match() -> () { meta: OpInfo { memory: Known([]) }, mnemonic: "match", storage: Match {} }
