// ----- constant-folding-preserves-precedence-and-short-circuiting-1
// run: opgen-error
// check: constraint is always false: ((1 + (2 * 3)) == 9)
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [1 + 2 * 3 == 9]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-1
// run: opgen-error
// check: constraint expects Int, got Bool
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [number && flag]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-2
// run: opgen-error
// check: constraint expects Int, got Bool
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [number == flag]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-3
// run: opgen-error
// check: unknown constraint name unknown
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [unknown != 0]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-4
// run: opgen-error
// check: field access requires a record
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [number.field != 0]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-5
// run: opgen-error
// check: all expects a finite sequence
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [all(number, |x| true)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-6
// run: opgen-error
// check: all expects a sequence and |name| predicate
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [all(number, flag)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-7
// run: opgen-error
// check: len expects a sequence
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [len(flag) == 0]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-8
// run: opgen-error
// check: type expects an SSA value or typed property
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [type(number) == result_type(0)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-9
// run: opgen-error
// check: result_type index is out of bounds
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [result_type(1) == result_type(0)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-10
// run: opgen-error
// check: unknown comparison variant IntCC.Unknown
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [IntCC.Unknown == IntCC.Eq]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-11
// run: opgen-error
// check: constraint expects Enum("IntCC"), got Enum("FloatCC")
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [IntCC.Eq == FloatCC.Eq]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-12
// run: opgen-error
// check: unknown constraint operation recurse
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [recurse(number)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-13
// run: opgen-error
// check: constraint expects Int, got Bool
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [flag + flag]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-14
// run: opgen-error
// check: require expects a predicate and a diagnostic string
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [require(flag)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-15
// run: opgen-error
// check: unknown constraint name VectorConstant
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [VectorConstant]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-16
// run: opgen-error
// check: unknown constraint operation nonzero
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [nonzero(number)]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-17
// run: opgen-error
// check: constraint arithmetic overflow
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [170141183460469231731687303715884105727 + 1 == 0]
}

// ----- ill-typed-or-unbounded-expressions-are-definition-errors-18
// run: opgen-error
// check: unknown constraint name missing
record Custom { bits: u64, yes: bool }
op Example(@number: u64, @flag: bool) -> ScalarInteger {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { bits: number, yes: flag },
     constraints: [true || missing > 0]
}

// ----- pool-properties-and-lexical-binders-are-generic-1
// run: opgen-error
// check: constraint expects Bool, got Int
record Custom { pool_id: ConstantPoolId }
op Example(@data: Bytes) -> Vector {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { pool_id: pool(data) },
    text: "{data:bytes}",
    constraints: [len(data) == min_bytes(result_type(0)), all(data, |i| i)]
}

// ----- pool-properties-and-lexical-binders-are-generic-2
// run: opgen-error
// check: result_type index is out of bounds
record Custom { pool_id: ConstantPoolId }
op Example(@data: Bytes) -> () {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Custom { pool_id: pool(data) },
    text: "{data:bytes}",
    constraints: [len(data) == min_bytes(result_type(0)), all(data, |i| i < 2 * lanes(result_type(0)))]
}

// ----- record-access-uses-logical-names-not-rule-or-storage-names-1
// run: opgen-error
// check: unknown field PtrIndexImm.unknown
record PtrIndexImm {
    offset: i32,
    scale: u32,
}

record PtrIndex {
    ptr: Value,
    index: Value,
    imm_id: PtrIndexImm,
}

op PtrIndex(ptr: PTR, index: ScalarInteger, @imm: PtrIndexImm) -> PTR {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "ptr-index",
    storage: PtrIndex { ptr: ptr, index: index, imm_id: imm },
    text: "{ptr}, {index}, scale={imm.scale}, offset={imm.offset}",

    constraints: [require(imm.unknown != 0, "ptr-index scale must be non-zero")]
}

// ----- type-queries-use-static-signatures-and-operand-positions-1
// run: opgen-error
// check: constraint is always false: is_ptr(type(input))
record Unary { arg: Value }
op Example<T: Integer>(input: T) -> T {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "example", storage: Unary { arg: input },
    constraints: [is_ptr(type(input)), lanes(type(input)) > 0]
}
