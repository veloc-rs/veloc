// ----- a-fixed-operand-after-a-variadic-group-is-an-explicit-definition-error-1
// run: opgen-error
// check: fixed operands must precede variadic operands and successors
struct CallIndirect {
    ptr: Value,
    args: ValueList,
    sig_id: SigId,
}
op CallIndirect(@sig_id: SigId, args: values, ptr: PTR) -> signature {
    meta: OpInfo { traits: [MAY_TRAP], memory: Unknown },
    mnemonic: "call-indirect",
    storage: CallIndirect { ptr: ptr, args: args, sig_id: sig_id },
    text: "{ptr}({args}) : {sig_id}",
    signature: sig_id,
}

// ----- anonymous-results-have-no-synthetic-names-1
// run: opgen-error
// check: unknown expression name or operation: result
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> Integer {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
verify {
        require(element_bits(type(result)) > element_bits(type(left)), "result must have more bits per lane than left");
    } }

// ----- anonymous-results-have-no-synthetic-names-2
// run: opgen-error
// check: unknown expression name or operation: result0
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> Integer {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
verify {
        require(element_bits(type(result0)) > element_bits(type(left)), "result0 must have more bits per lane than left");
    } }

// ----- bare-result-types-leave-the-operation-body-unconsumed-1
// run: opgen-error
// check: expected a name
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) ->  {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
}

// ----- bare-result-types-leave-the-operation-body-unconsumed-2
// run: opgen-error
// check: expected `,`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (T {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
}

// ----- bare-result-types-leave-the-operation-body-unconsumed-3
// run: opgen-error
// check: expected `{`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> T, BOOL {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-4
// run: opgen-error
// check: expected `,`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (T BOOL) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-5
// run: opgen-error
// check: expected a name
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result:) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-6
// run: opgen-error
// check: expected a result name before ':'
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (shape(T, Integer): T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-7
// run: opgen-error
// check: results cannot be properties
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (@result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-8
// run: opgen-error
// check: unknown type pattern
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> ([T]) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- bare-result-types-leave-the-operation-body-unconsumed-9
// run: opgen-error
// check: expected `{`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> result: T {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-1
// run: opgen-error
// check: duplicate type variable `T`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer, T: Float>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-2
// run: opgen-error
// check: unknown type or class `Missing`
struct Pair {
    args: values(2),
}
op PairAdd<T: Missing>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-3
// run: opgen-error
// check: unbound type variable `Undeclared`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: Undeclared) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-4
// run: opgen-error
// check: impossible element constraint on type variable `T`
struct Pair {
    args: values(2),
}
op PairAdd<T: ScalarInteger>(left: T, right: element(T)) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-5
// run: opgen-error
// check: impossible vector constraint on type variable `T`
struct Pair {
    args: values(2),
}
op PairAdd<T: Vector>(left: T, right: vector(T)) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- generic-declarations-and-type-domains-are-checked-6
// run: opgen-error
// check: impossible shape constraint on type variable `T`
struct Pair {
    args: values(2),
}
op PairAdd<T: ScalarInteger>(left: T, right: shape(T, Vector)) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- operand-and-result-names-must-be-unique-1
// run: opgen-error
// check: duplicate parameter `left`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, left: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- operand-and-result-names-must-be-unique-2
// run: opgen-error
// check: duplicate parameter or result `left`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (left: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- operand-and-result-names-must-be-unique-3
// run: opgen-error
// check: duplicate parameter or result `result`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result: T, result: BOOL) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- operand-and-result-names-must-be-unique-4
// run: opgen-error
// check: duplicate parameter `ptr`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(ptr: PTR, @ptr: u32, @flags: MemFlags) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset, flags: flags },
    text: "{.flags} {ptr}, offset={offset}",
     }

// ----- properties-must-match-their-declared-storage-type-and-role-1
// run: opgen-error
// check: parameter `offset` is incompatible with storage field `offset`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(ptr: PTR, @offset: i32, @flags: MemFlags) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset, flags: flags },
    text: "{.flags} {ptr}, offset={offset}",
     }

// ----- properties-must-match-their-declared-storage-type-and-role-2
// run: opgen-error
// check: parameter `flags` is incompatible with storage field `flags`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(ptr: PTR, @offset: u32, @flags: bool) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset, flags: flags },
    text: "{.flags} {ptr}, offset={offset}",
     }

// ----- properties-must-match-their-declared-storage-type-and-role-3
// run: opgen-error
// check: parameter `offset` is incompatible with storage field `offset`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(ptr: PTR, offset: I32, @flags: MemFlags) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset, flags: flags },
    text: "{.flags} {ptr}, offset={offset}",
     }

// ----- properties-must-match-their-declared-storage-type-and-role-4
// run: opgen-error
// check: parameter `ptr` is incompatible with storage field `ptr`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(@ptr: u32, @offset: u32, @flags: MemFlags) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset, flags: flags },
    text: "{.flags} {ptr}, offset={offset}",
     }

// ----- properties-must-match-their-declared-storage-type-and-role-5
// run: opgen-error
// check: parameter `args` is incompatible with storage field `args`
struct CallIndirect {
    ptr: Value,
    args: ValueList,
    sig_id: SigId,
}
op CallIndirect(@sig_id: SigId, ptr: PTR, args: successor) -> signature {
    meta: OpInfo { traits: [MAY_TRAP], memory: Unknown },
    mnemonic: "call-indirect",
    storage: CallIndirect { ptr: ptr, args: args, sig_id: sig_id },
    text: "{ptr}({args}) : {sig_id}",
    signature: sig_id,
     }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-1
// run: opgen-error
// check: missing storage field `args`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair {  },
    }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-2
// run: opgen-error
// check: parameter `left` is stored more than once
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, left] },
    }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-3
// run: opgen-error
// check: unknown parameter `unknown` in storage mapping
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, unknown] },
    }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-4
// run: opgen-error
// check: unknown storage field `extra` in `Pair`
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right], extra: left },
    }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-5
// run: opgen-error
// check: parameter `unused` has no storage mapping
struct Pair {
    args: values(2),
}
op PairAdd<T: Integer>(left: T, right: T, unused: T) -> (result: T) {
    meta: OpInfo { memory: Known([]) },
    mnemonic: "pair-add",
    storage: Pair { args: [left, right] },
    }

// ----- storage-packing-covers-every-field-and-logical-parameter-once-6
// run: opgen-error
// check: missing storage field `flags`
struct Load {
    ptr: Value,
    offset: u32,
    flags: MemFlags,
}
op Load(ptr: PTR, @offset: u32, @flags: MemFlags) -> (result: Any) {
    meta: OpInfo { traits: [MAY_TRAP], memory: Known([READ]) },
    mnemonic: "load",
    storage: Load { ptr: ptr, offset: offset },
    text: "{.flags} {ptr}, offset={offset}",
     }
