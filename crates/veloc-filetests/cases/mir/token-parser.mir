// ----- tight-punctuation-forward-calls-and-multiple-results
// run: roundtrip
// check: (v1: i32, v2: i32) = call later(v0) : (i32) -> (i32, i32)
// check: return v1, v2
local function main(i32)->(i32,i32)
block0(v0:i32):
  (v1: i32, v2: i32) = call later(v0):(i32)->(i32,i32)// forward function, no whitespace required
  return v1,v2
local function later(i32)->(i32,i32)
block0(v0:i32):
  return v0,v0

// ----- utf8-names-and-nested-signature
// run: roundtrip
// check: call-indirect 入口.v0(值.v1) : (i32) -> (i32, i32)
local function 调用(ptr,i32)->(i32,i32)
block0(入口:ptr,值:i32):
  (v2: i32, v3: i32) = call-indirect 入口(值):(i32)->(i32,i32)
  return v2,v3

// ----- nested-successors-and-comments
// run: roundtrip
// check: br-table v0, [block1(v1), block2(v1)], block2(v1)
local function table(i32,i32)->i32
block0(v0:i32,v1:i32):
  br-table v0,[block1(v1),block2(v1)],block2(v1) // ) ] , = ignored
block1(v2:i32):
  jump block2(v2)
block2(v3:i32):
  return v3

// ----- scalable-types-and-reordered-fields
// run: roundtrip
// check: : i32<scalable 4> = load-stride.volatile.align4
// check: stride=v1, offset=-16, mask=v2
local function vector(ptr,i32,mask<scalable 4>)->i32<scalable 4>
block0(v0:ptr,v1:i32,v2:mask<scalable 4>):
  v3: i32<scalable 4> = load-stride.volatile.align4 v0,mask=v2,offset=-16,stride=v1
  return v3

// ----- global-and-declaration-like-value-names
// run: roundtrip
// check: global counter: i32 (local)
// check: ssvalue.v0: i32 = iconst 1
// check: blockvalue.v1: i32 = iadd ssvalue.v0, ssvalue.v0
global counter:i32(local)
local function names()->i32
block0():
  ssvalue: i32 = iconst 1
  blockvalue: i32 = iadd ssvalue,ssvalue
  return blockvalue

// ----- missing-comma-location
// run: parse-error
// check: line 6, column 21
// check: expected `,`
local function bad(i32)->i32
block0(v0:i32):
  v1: i32 = iadd v0 v0

// ----- newline-does-not-continue-operands
// run: parse-error
// check: line 6
// check: SSA value
local function bad(i32)->i32
block0(v0:i32):
  v1: i32 = iadd v0,
  return v0

// ----- nested-call-cannot-hide-trailing-text
// run: parse-error
// check: unexpected operand or trailing text
local function bad()->void
block0():
  jump block1() extra
block1():
  return

// ----- unmatched-call-delimiter
// run: parse-error
// check: RParen
local function bad()->void
block0():
  jump block1(
block1():
  return

// ----- unmatched-list-delimiter
// run: parse-error
// check: RBracket
local function bad(i32)->void
block0(v0:i32):
  br-table v0,[block1()),block1()
block1():
  return

// ----- trailing-comma-before-optional-fields
// run: parse-error
// check: expected name or operand
local function bad(ptr)->i32
block0(v0:ptr):
  v1: i32 = load v0,
  return v1

// ----- trailing-comma-after-named-fields
// run: parse-error
// check: expected name or operand
local function bad(ptr)->i32
block0(v0:ptr):
  v1: i32 = load v0,offset=4,
  return v1

// ----- missing-value-in-named-field
// run: parse-error
// check: operand `offset`
local function bad(ptr)->i32
block0(v0:ptr):
  v1: i32 = load v0,offset=
  return v1

// ----- trailing-comma-in-successor-list
// run: parse-error
// check: expected name or operand
local function bad(i32)->void
block0(v0:i32):
  br-table v0,[block1(),],block1()
block1():
  return

// ----- malformed-vector-type
// run: parse-error
// check: Greater
local function bad(i32<4])->void

// ----- legacy-result-type-suffix-is-rejected
// run: parse-error
// check: unknown opcode suffix `i32`
local function bad()->i32
block0():
  v0: i32 = iconst.i32 1
  return v0

// ----- missing-arrow-in-nested-signature
// run: parse-error
// check: Arrow
local function bad(ptr,i32)->i32
block0(v0:ptr,v1:i32):
  v2:i32=call-indirect v0(v1):(i32) i32
  return v2

// ----- trailing-text-in-function-header
// run: parse-error
// check: unexpected operand or trailing text
local function bad()->void extra

// ----- leading-dot-value-is-not-a-suffix
// run: roundtrip
// check: .value.v1: i32 = iadd .input.v0, .input.v0
local function dots(i32)->i32
block0(.input:i32):
  .value: i32 = iadd .input,.input
  return .value

// ----- trailing-text-in-global
// run: parse-error
// check: unexpected operand or trailing text
global counter:i32(local) extra

// ----- trailing-text-in-block-header
// run: parse-error
// check: unexpected operand or trailing text
local function bad()->void
block0(): extra

// ----- trailing-text-in-stack-slot
// run: parse-error
// check: unexpected operand or trailing text
local function bad()->void
ss0: size 8 extra

// ----- trailing-text-after-zero-operand-op
// run: parse-error
// check: unexpected operand or trailing text
local function bad()->void
block0():
  unreachable extra

// ----- unicode-literal-error-location
// run: parse-error
// check: line 6, column 19
// check: invalid integer constant
local function bad()->i32
block0():
  值: i32 = iconst nope
